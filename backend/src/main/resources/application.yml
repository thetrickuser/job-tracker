spring:
  application:
    name: jobtrack
  datasource:
    url: jdbc:postgresql://postgres:5432/jobtrackdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration  